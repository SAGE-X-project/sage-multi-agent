# ---------- Host ----------
HOST=localhost

# ---------- Root & Client ----------
ROOT_AGENT_PORT=18080
CLIENT_PORT=8086

# ---------- Gateway (reverse proxy that may tamper) ----------
GATEWAY_PORT=5500

# ---------- External Payment Agent (verifies signatures) ----------
EXT_PAYMENT_PORT=19083

# ---------- Internal Payment Agent (HTTP debug server; 일부 스크립트에서 사용) ----------
PAYMENT_AGENT_PORT=18083

# ---------- Payment outbound signing (Payment -> Gateway/External) ----------
# Where PaymentAgent sends outbound /process (via gateway)
PAYMENT_EXTERNAL_URL=http://localhost:5500

# If SAGE is ON, key is required:
PAYMENT_JWK_FILE=./keys/payment.jwk
EXTERNAL_JWK_FILE=./keys/external.jwk
EXTERNAL_KEM_JWK_FILE=keys/kem/external.x25519.jwk
# (optional) Explicit DID; 없으면 secp256k1 JWK로부터 주소 기반 DID 유도
# PAYMENT_DID=did:sage:ethereum:0x1111111111111111111111111111111111111111

# ---------- Debug: sub-agent SAGE toggles (지금은 IN-PROC이므로 영향 거의 없음) ----------
PLANNING_SAGE_ENABLED=false
ORDERING_SAGE_ENABLED=false
PAYMENT_SAGE_ENABLED=true
